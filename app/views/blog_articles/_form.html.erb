<%= form_with(model: blog_article, local: true) do |form| %>
  <% if blog_article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog_article.errors.count, "error") %> prohibited this blog_article from being saved:</h2>

      <ul>
      <% blog_article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
      
    <div class="col-sm-12">
      <div class="form-group">
        <%= form.label :title, 'Titulo' %>
        <%= form.text_field :title, id: :blog_article_title, class: 'form-control' %>
      </div>
    </div>

    <div class="col-sm-12">
      <div class="form-group">
        <%= form.label :content, 'Contenido o descripción' %>
        <%= form.text_area :content, id: :blog_article_content, class: 'form-control' %>
      </div>
    </div>

    <div class="col-sm-12">
      <div class="form-group">
        <%= form.label :url, 'URL' %>
        <%= form.text_field :url, id: :blog_article_url, class: 'form-control' %>
      </div>
    </div>

    <div class="col-sm-12">
      <div class="form-group">
        <%= form.label :source, 'Fuente de la noticia' %>
        <%= form.text_field :source, id: :blog_article_source, class: 'form-control' %>
      </div>
    </div>

    <div class="col-sm-12">
      <div class="form-group">
        <%= form.label :image_url, 'URL de la Imagen' %>
        <%= form.text_field :image_url, id: :blog_article_image_url, class: 'form-control' %>
      </div>
    </div>

    <div class="col-sm-12 text-right">
      <div class="actions">
        <%= form.submit class: 'btn btn-primary' %>
      </div>
    </div>
  </div>
<% end %>
