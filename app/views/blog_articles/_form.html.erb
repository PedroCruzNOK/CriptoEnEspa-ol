<%= form_with(model: blog_article, local: true) do |form| %>
  <% if blog_article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog_article.errors.count, "error") %> prohibited this blog_article from being saved:</h2>

      <ul>
      <% blog_article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :blog_article_title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :blog_article_content %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url, id: :blog_article_url %>
  </div>

  <div class="field">
    <%= form.label :source %>
    <%= form.text_field :source, id: :blog_article_source %>
  </div>

  <div class="field">
    <%= form.label :image_url %>
    <%= form.text_field :image_url, id: :blog_article_image_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
